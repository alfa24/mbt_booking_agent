# Task: Удаление глобального чат-виджета

## Цель

Удалить плавающий чат-виджет из приложения.

## Выполненные действия

1. **Изменен `web/src/app/(authenticated)/layout.tsx`**
   - Удален импорт `ChatWidget` из `@/components/chat/chat-widget`
   - Удален рендер `<ChatWidget />` из JSX

2. **Удалены файлы:**
   - `web/src/components/chat/chat-widget.tsx` — компонент плавающего виджета
   - `web/src/components/chat/chat-window.tsx` — компонент окна чата

3. **Проверка**
   - Grep по проекту подтверждает отсутствие других импортов удаленных компонентов

## Результат

Плавающий чат-виджет полностью удален. Полностраничный чат (`/chat`) продолжает работать на оставшихся компонентах в `web/src/components/chat/`.
